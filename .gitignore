node_modules/
dist/
.env
*.db
.vercel/

# No Python ships in this repo any more (the backend/ tree was deleted 2026-09-18),
# but export_excel.py and generate_schema_v2.py below are still run locally. Keep
# these: ten .pyc files were once committed, and a gitignore rule does not untrack
# a file that is already tracked.
__pycache__/
*.pyc

FarmApp_Database.xlsx
FarmApp_Schema_v2.xlsx
FarmApp_AppFlow.html
export_excel.py
generate_schema_v2.py
frontend/.env.local

# Holds a Supabase personal access token. Never commit.
# See .mcp.json.example for the shape.
.mcp.json
